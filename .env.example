# Vyline 環境変数（例）
# コピーして .env として使用。全て任意。

# ── バックエンド ──────────────────────────────
# PORT=3001
# bind アドレス（Docker / 外部公開時は 0.0.0.0）
# VYLINE_HOST=127.0.0.1
# LAN内のスマホ等から接続する場合だけ true（PC設定からQRペアリングを開始）
# VYLINE_LAN_ACCESS=false
# リモートブラウザをPCオーナー権限として扱う明示的なバイパス。
# Tailscale ACL / Cloudflare Access / 認証済みリバースプロキシ等で到達経路自体を
# 保護している場合だけ true にすること。生のLAN/インターネット公開では使用しない。
# VYLINE_TRUST_REMOTE_OWNER=false
# データディレクトリ（トークン / 履歴 / キャッシュ）
# VYLINE_DATA_DIR=backend/data
# スタンプ / sticon キャッシュ
# VYLINE_CDN_CACHE_DIR=backend/data/cdn-cache
# 画像 / 動画メディアキャッシュ
# VYLINE_MEDIA_CACHE_DIR=backend/data/media-cache
# CORS 許可オリジン（dev は Vite 5173）
# VYLINE_CORS_ORIGIN=http://localhost:5173
# 本番配信するフロントビルドの場所
# VYLINE_STATIC_DIR=Vyline/apps/desktop/dist

# ── デバイス種別 ──────────────────────────────
# VYLINE_DEVICE=IOSIPAD

# ── Discord サーバー構築（実行時のみ設定。トークンは絶対にコミットしない） ──
# DISCORD_BOT_TOKEN=
# DISCORD_GUILD_ID=
