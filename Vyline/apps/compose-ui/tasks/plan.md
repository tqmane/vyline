# KMP mobile repair

The 2026-09-08 request and attached screenshots/videos are the acceptance contract. Preserve the existing Zustand/controllers/API/protocol. Use installed Compose libraries and their version-matched local source; visible KMP presentation must not fall back to legacy React.

Order: input boundaries and tab geometry; timeline anchoring and composer/navigation; native presentation parity; hydration cache; development and production browser validation. Independent read-only review checks the input boundaries while implementation proceeds. No LINE messages are sent to real contacts during fixture checks.

Verification: current Kotlin compile, development distribution + `smoke.mjs --chat --motion --regressions`, production distribution + same suite with `--production`, desktop production build. Add focused mobile touch, label geometry, incoming/self-send, reload/theme cache observations and record actual results in MOTION-VALIDATION.md. Browser-emulated touch does not prove physical Android IME behavior.

Tasks and completion evidence: [todo.md](todo.md).
